---
title: "Instagram Post"
description: "Instagram post card with image and engagement."
base: base
component: true
---

<ComponentPreview
  name="instagram-post"
  src="examples/instagram-post-demo.tsx"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://mcpcn.dev/r/instagram-post.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install lucide-react
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource
  name="instagram-post"
  title="components/ui/instagram-post.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { InstagramPost } from "@/components/ui/instagram-post";
```

```tsx
<InstagramPost />
```

## Composition

Use the following composition to build an `InstagramPost`:

```text
InstagramPost
└── InstagramPost.Content
```

## Examples

### With a custom heading

This example adds a custom heading before the component's `Content`. `Content` reads the root props from compound context, so the original component view needs no repeated props.

<ComponentPreview
  name="instagram-post"
  src="examples/instagram-post-demo.tsx"
  variant="composed"
  showCode
/>

## Integrate with OpenUI

Register `InstagramPost` with `defineComponent`, describe its model-generated props with Zod, and include it in a library.

```tsx showLineNumbers
import { createLibrary, defineComponent } from "@openuidev/react-lang";
import { z } from "zod/v4";

import { InstagramPost } from "@/components/ui/instagram-post";

const OpenUIInstagramPost = defineComponent({
  name: "InstagramPost",
  description: "Instagram post card with image and engagement.",
  props: z.object({}),
  component: () => <InstagramPost />,
});

export const instagram_postLibrary = createLibrary({
  root: "InstagramPost",
  components: [OpenUIInstagramPost],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## API Reference

See the [Base UI](https://base-ui.com/react/overview/quick-start) documentation for more information.
