---
title: "Product List"
description: "Product list with list, grid, carousel, and picker variants."
base: base
component: true
---

<ComponentPreview name="product-list" src="examples/product-list-demo.tsx" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://mcpcn.dev/r/product-list.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install lucide-react
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource name="product-list" title="components/ui/product-list.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ProductList, ProductListContent } from "@/components/ui/product-list";
```

```tsx
<ProductList>
  <ProductListContent />
</ProductList>
```

## Composition

Use the following composition to build an `ProductList`:

```text
ProductList
└── ProductListContent
```

## Examples

### Grid

<ComponentPreview
  name="product-list-grid"
  src="examples/product-list-grid-demo.tsx"
  showCode
/>

### Carousel

<ComponentPreview
  name="product-list-carousel"
  src="examples/product-list-carousel-demo.tsx"
  showCode
/>

### Picker

<ComponentPreview
  name="product-list-picker"
  src="examples/product-list-picker-demo.tsx"
  showCode
/>

## OpenUI Integration

Register `ProductList` with `defineComponent`, describe its model-generated props with Zod, and include it in a library.

```tsx showLineNumbers
import { ProductList, ProductListContent } from "@/components/ui/product-list";

const OpenUIProductList = defineComponent({
  name: "ProductList",
  description: "Product list with list, grid, carousel, and picker variants.",
  props: z.object({}),
  component: () => (
    <ProductList>
      <ProductListContent />
    </ProductList>
  ),
});

export const product_listLibrary = createLibrary({
  root: "ProductList",
  components: [OpenUIProductList],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## API Reference

See the [Base UI](https://base-ui.com/react/overview/quick-start) documentation for more information.
