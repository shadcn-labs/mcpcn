---
title: "Post Card"
description: "Post card with image, title, excerpt, author info and read more button. Supports default, compact, horizontal, and covered variants. Includes post-detail for fullscreen view."
base: base
component: true
---

<ComponentPreview name="post-card" src="examples/post-card-demo.tsx" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://mcpcn.dev/r/post-card.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install lucide-react
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource name="post-card" title="components/ui/post-card.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { PostCard } from "@/components/ui/post-card";
```

```tsx
<PostCard />
```

## Composition

Use the following composition to build an `PostCard`:

```text
PostCard
├── PostCardAction
├── PostCardAuthor
├── PostCardCategory
├── PostCardContent
├── PostCardImage
├── PostCardTags
└── PostCardText
```

## Integrate with OpenUI

Register `PostCard` with `defineComponent`, describe its model-generated props with Zod, and include it in a library.

```tsx showLineNumbers
import { createLibrary, defineComponent } from "@openuidev/react-lang";
import { z } from "zod/v4";

import { PostCard } from "@/components/ui/post-card";

const OpenUIPostCard = defineComponent({
  name: "PostCard",
  description:
    "Post card with image, title, excerpt, author info and read more button. Supports default, compact, horizontal, and covered variants. Includes post-detail for fullscreen view.",
  props: z.object({}),
  component: () => <PostCard />,
});

export const post_cardLibrary = createLibrary({
  root: "PostCard",
  components: [OpenUIPostCard],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## Examples

### Without Image

<ComponentPreview
  name="post-card-without-image"
  src="examples/post-card-without-image-demo.tsx"
  showCode
/>

### Compact

<ComponentPreview
  name="post-card-compact"
  src="examples/post-card-compact-demo.tsx"
  showCode
/>

### Horizontal

<ComponentPreview
  name="post-card-horizontal"
  src="examples/post-card-horizontal-demo.tsx"
  showCode
/>

### Covered

<ComponentPreview
  name="post-card-covered"
  src="examples/post-card-covered-demo.tsx"
  showCode
/>

## API Reference

See the [Base UI](https://base-ui.com/react/overview/quick-start) documentation for more information.
