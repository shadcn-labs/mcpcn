---
title: "Post List"
description: "Post list with list, grid, carousel, and fullwidth variants. Fullwidth mode shows paginated posts."
base: base
component: true
---

<ComponentPreview name="post-list" src="examples/post-list-demo.tsx" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @mcpcn/post-list
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install lucide-react
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource name="post-list" title="components/ui/post-list.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { PostList, PostListContent } from "@/components/ui/post-list";
```

```tsx
<PostList>
  <PostListContent />
</PostList>
```

## Composition

Use the following composition to build an `PostList`:

```text
PostList
└── PostListContent
```

## Examples

### Grid

<ComponentPreview
  name="post-list-grid"
  src="examples/post-list-grid-demo.tsx"
  showCode
/>

### Carousel

<ComponentPreview
  name="post-list-carousel"
  src="examples/post-list-carousel-demo.tsx"
  showCode
/>

## OpenUI Integration

Register `PostList` with `defineComponent`, describe its model-generated props with Zod, and include it in a library.

```tsx showLineNumbers
import { PostList, PostListContent } from "@/components/ui/post-list";

const OpenUIPostList = defineComponent({
  name: "PostList",
  description:
    "Post list with list, grid, carousel, and fullwidth variants. Fullwidth mode shows paginated posts.",
  props: z.object({}),
  component: () => (
    <PostList>
      <PostListContent />
    </PostList>
  ),
});

export const post_listLibrary = createLibrary({
  root: "PostList",
  components: [OpenUIPostList],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## API Reference

See the [Base UI](https://base-ui.com/react/overview/quick-start) documentation for more information.
