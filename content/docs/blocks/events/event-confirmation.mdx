---
title: "Event Confirmation"
description: "A composition-first event confirmation block for MCP Apps."
base: base
component: true
---

<ComponentPreview
  name="event-confirmation"
  src="examples/event-confirmation-demo.tsx"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://mcpcn.dev/r/event-confirmation.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install @base-ui/react lucide-react
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource
  name="event-confirmation"
  title="components/ui/event-confirmation.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import { EventConfirmation } from "@/components/ui/event-confirmation";
```

```tsx showLineNumbers
<EventConfirmation />
```

## Composition

Use the following compound-component composition to customize `EventConfirmation`:

```text
EventConfirmation
├── EventConfirmation.Header
├── EventConfirmation.Details
├── EventConfirmation.Organizer
└── EventConfirmation.Share
```

## Integrate with OpenUI

Register `EventConfirmation` with `defineComponent`, describe its model-generated props with Zod, and include it in a library. Keep the schema flat so OpenUI Lang can generate and stream it reliably.

```tsx showLineNumbers
import { createLibrary, defineComponent } from "@openuidev/react-lang";
import { z } from "zod/v4";

import { EventConfirmation } from "@/components/ui/event-confirmation";

const OpenUIEventConfirmation = defineComponent({
  name: "EventConfirmation",
  description: "Event Confirmation for an MCP-powered interface.",
  props: z.object({
    // Add flat, serializable props that the model should generate.
  }),
  component: ({ props }) => <EventConfirmation />,
});

export const eventconfirmationLibrary = createLibrary({
  root: "EventConfirmation",
  components: [OpenUIEventConfirmation],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## Examples

### Default and composed

The preview includes the ready-to-use default and a custom composition. Use the controls above the border to inspect inline, picture-in-picture, and full-width layouts.

<ComponentPreview
  name="event-confirmation"
  src="examples/event-confirmation-demo.tsx"
/>

## API Reference

The component is built on the Base UI foundation and ships as editable source. See the [registry item](/r/event-confirmation.json) for its files and dependencies.
