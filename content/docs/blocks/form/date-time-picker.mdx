---
title: "Date & Time Picker"
description: "A Calendly-style date and time picker with calendar, available time slots, and timezone display."
base: base
component: true
---

<ComponentPreview
  name="date-time-picker"
  src="examples/date-time-picker-demo.tsx"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @mcpcn/date-time-picker
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install lucide-react
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource
  name="date-time-picker"
  title="components/ui/date-time-picker.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import {
  DateTimePicker,
  DateTimePickerContent,
} from "@/components/ui/date-time-picker";
```

```tsx
<DateTimePicker>
  <DateTimePickerContent />
</DateTimePicker>
```

## Composition

Use the following composition to build an `DateTimePicker`:

```text
DateTimePicker
└── DateTimePickerContent
```

## OpenUI Integration

Install the OpenUI React packages.

```bash
pnpm add @openuidev/react-lang zod
```

Add `DateTimePicker` to a component library and map it to the installed component.

```tsx showLineNumbers
import { createLibrary, defineComponent } from "@openuidev/react-lang";
import { z } from "zod/v4";

import {
  DateTimePicker,
  DateTimePickerContent,
} from "@/components/ui/date-time-picker";

const OpenUIDateTimePicker = defineComponent({
  name: "DateTimePicker",
  description:
    "A Calendly-style date and time picker with calendar, available time slots, and timezone display.",
  props: z.object({}),
  component: () => (
    <DateTimePicker>
      <DateTimePickerContent />
    </DateTimePicker>
  ),
});

export const date_time_pickerLibrary = createLibrary({
  root: "DateTimePicker",
  components: [OpenUIDateTimePicker],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## json-render Integration

Install the json-render React packages.

```bash
pnpm add @json-render/core @json-render/react zod
```

Add `DateTimePicker` to a component catalog and map it to the installed component.

```tsx
import { defineCatalog } from "@json-render/core";
import { defineRegistry } from "@json-render/react";
import { schema } from "@json-render/react/schema";
import { z } from "zod";

import {
  DateTimePicker,
  DateTimePickerContent,
} from "@/components/ui/date-time-picker";

export const catalog = defineCatalog(schema, {
  components: {
    DateTimePicker: {
      props: z.object({}),
      description:
        "A Calendly-style date and time picker with calendar, available time slots, and timezone display.",
    },
  },
});

export const { registry } = defineRegistry(catalog, {
  components: {
    DateTimePicker: () => (
      <DateTimePicker>
        <DateTimePickerContent />
      </DateTimePicker>
    ),
  },
});
```

Use `catalog.prompt()` to constrain model output, then render the generated spec with the json-render React `Renderer`. See the [json-render Quick Start](https://json-render.dev/docs/quick-start) for the provider and streaming setup.

## API Reference

See the [Base UI](https://base-ui.com/react/overview/quick-start) documentation for more information.
