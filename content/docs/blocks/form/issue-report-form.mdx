---
title: "Issue Report Form"
description: "A compact issue reporting form for team members with categories, impact/urgency levels, and file attachments."
base: base
component: true
---

<ComponentPreview
  name="issue-report-form"
  src="examples/issue-report-form-demo.tsx"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @mcpcn/issue-report-form
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install lucide-react
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource
  name="issue-report-form"
  title="components/ui/issue-report-form.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import {
  IssueReportForm,
  IssueReportFormContent,
} from "@/components/ui/issue-report-form";
```

```tsx
<IssueReportForm>
  <IssueReportFormContent />
</IssueReportForm>
```

## Composition

Use the following composition to build an `IssueReportForm`:

```text
IssueReportForm
└── IssueReportFormContent
```

## OpenUI Integration

Install the OpenUI React packages.

```bash
pnpm add @openuidev/react-lang zod
```

Add `IssueReportForm` to a component library and map it to the installed component.

```tsx showLineNumbers
import { createLibrary, defineComponent } from "@openuidev/react-lang";
import { z } from "zod/v4";

import {
  IssueReportForm,
  IssueReportFormContent,
} from "@/components/ui/issue-report-form";

const OpenUIIssueReportForm = defineComponent({
  name: "IssueReportForm",
  description:
    "A compact issue reporting form for team members with categories, impact/urgency levels, and file attachments.",
  props: z.object({}),
  component: () => (
    <IssueReportForm>
      <IssueReportFormContent />
    </IssueReportForm>
  ),
});

export const issue_report_formLibrary = createLibrary({
  root: "IssueReportForm",
  components: [OpenUIIssueReportForm],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## json-render Integration

Install the json-render React packages.

```bash
pnpm add @json-render/core @json-render/react zod
```

Add `IssueReportForm` to a component catalog and map it to the installed component.

```tsx
import { defineCatalog } from "@json-render/core";
import { defineRegistry } from "@json-render/react";
import { schema } from "@json-render/react/schema";
import { z } from "zod";

import {
  IssueReportForm,
  IssueReportFormContent,
} from "@/components/ui/issue-report-form";

export const catalog = defineCatalog(schema, {
  components: {
    IssueReportForm: {
      props: z.object({}),
      description:
        "A compact issue reporting form for team members with categories, impact/urgency levels, and file attachments.",
    },
  },
});

export const { registry } = defineRegistry(catalog, {
  components: {
    IssueReportForm: () => (
      <IssueReportForm>
        <IssueReportFormContent />
      </IssueReportForm>
    ),
  },
});
```

Use `catalog.prompt()` to constrain model output, then render the generated spec with the json-render React `Renderer`. See the [json-render Quick Start](https://json-render.dev/docs/quick-start) for the provider and streaming setup.

## API Reference

See the [Base UI](https://base-ui.com/react/overview/quick-start) documentation for more information.
