---
title: "Map Carousel"
description: "Interactive map with location markers and a draggable carousel of cards."
base: base
component: true
---

<ComponentPreview name="map-carousel" src="examples/map-carousel-demo.tsx" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @mcpcn/map-carousel
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install lucide-react leaflet
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource name="map-carousel" title="components/ui/map-carousel.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { MapCarousel, MapCarouselContent } from "@/components/ui/map-carousel";
```

```tsx
<MapCarousel>
  <MapCarouselContent />
</MapCarousel>
```

## Composition

Use the following composition to build an `MapCarousel`:

```text
MapCarousel
└── MapCarouselContent
```

## OpenUI Integration

Install the OpenUI React packages.

```bash
pnpm add @openuidev/react-lang zod
```

Add `MapCarousel` to a component library and map it to the installed component.

```tsx showLineNumbers
import { createLibrary, defineComponent } from "@openuidev/react-lang";
import { z } from "zod/v4";

import { MapCarousel, MapCarouselContent } from "@/components/ui/map-carousel";

const OpenUIMapCarousel = defineComponent({
  name: "MapCarousel",
  description:
    "Interactive map with location markers and a draggable carousel of cards.",
  props: z.object({}),
  component: () => (
    <MapCarousel>
      <MapCarouselContent />
    </MapCarousel>
  ),
});

export const map_carouselLibrary = createLibrary({
  root: "MapCarousel",
  components: [OpenUIMapCarousel],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## json-render Integration

Install the json-render React packages.

```bash
pnpm add @json-render/core @json-render/react zod
```

Add `MapCarousel` to a component catalog and map it to the installed component.

```tsx
import { defineCatalog } from "@json-render/core";
import { defineRegistry } from "@json-render/react";
import { schema } from "@json-render/react/schema";
import { z } from "zod";

import { MapCarousel, MapCarouselContent } from "@/components/ui/map-carousel";

export const catalog = defineCatalog(schema, {
  components: {
    MapCarousel: {
      props: z.object({}),
      description:
        "Interactive map with location markers and a draggable carousel of cards.",
    },
  },
});

export const { registry } = defineRegistry(catalog, {
  components: {
    MapCarousel: () => (
      <MapCarousel>
        <MapCarouselContent />
      </MapCarousel>
    ),
  },
});
```

Use `catalog.prompt()` to constrain model output, then render the generated spec with the json-render React `Renderer`. See the [json-render Quick Start](https://json-render.dev/docs/quick-start) for the provider and streaming setup.

## API Reference

See the [Base UI](https://base-ui.com/react/overview/quick-start) documentation for more information.
