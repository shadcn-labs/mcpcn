---
title: "Order Confirm"
description: "Order confirmation with product image, delivery info, and confirm action."
base: base
component: true
---

<ComponentPreview name="order-confirm" src="examples/order-confirm-demo.tsx" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://mcpcn.dev/r/order-confirm.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the component dependencies:</Step>

```bash
npm install lucide-react
```

<Step>Copy and paste the component into your project.</Step>

<ComponentSource name="order-confirm" title="components/ui/order-confirm.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import {
  OrderConfirm,
  OrderConfirmContent,
  OrderConfirmFooter,
  OrderConfirmProduct,
} from "@/components/ui/order-confirm";
```

```tsx
<OrderConfirm>
  <OrderConfirmContent>
    <OrderConfirmProduct />
    <div className="border-t" />
    <OrderConfirmFooter />
  </OrderConfirmContent>
</OrderConfirm>
```

## Composition

Use the following composition to build an `OrderConfirm`:

```text
OrderConfirm
├── OrderConfirmAction
├── OrderConfirmContent
├── OrderConfirmDelivery
├── OrderConfirmFooter
└── OrderConfirmProduct
```

## OpenUI Integration

Register `OrderConfirm` with `defineComponent`, describe its model-generated props with Zod, and include it in a library.

```tsx showLineNumbers
import {
  OrderConfirm,
  OrderConfirmContent,
  OrderConfirmFooter,
  OrderConfirmProduct,
} from "@/components/ui/order-confirm";

const OpenUIOrderConfirm = defineComponent({
  name: "OrderConfirm",
  description:
    "Order confirmation with product image, delivery info, and confirm action.",
  props: z.object({}),
  component: () => (
    <OrderConfirm>
      <OrderConfirmContent>
        <OrderConfirmProduct />
        <div className="border-t" />
        <OrderConfirmFooter />
      </OrderConfirmContent>
    </OrderConfirm>
  ),
});

export const order_confirmLibrary = createLibrary({
  root: "OrderConfirm",
  components: [OpenUIOrderConfirm],
});
```

For richer generated compositions, define the child primitives separately and connect them with `.ref` fields. See [Defining Components](https://www.openui.com/docs/openui-lang/defining-components) in the OpenUI documentation.

## API Reference

See the [Base UI](https://base-ui.com/react/overview/quick-start) documentation for more information.
