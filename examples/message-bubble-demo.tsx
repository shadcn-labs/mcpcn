"use client";

import {
  MessageBubble,
  MessageBubbleContent,
} from "@/registry/messaging/message-bubble";

export default function MessageBubbleDemo() {
  return (
    <MessageBubble>
      <MessageBubbleContent />
    </MessageBubble>
  );
}
